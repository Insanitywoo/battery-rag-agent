[project]
name = "battery-rag-agent-backend"
version = "0.1.0"
description = "FastAPI shell for the Battery-RAG Agent bootstrap."
requires-python = ">=3.13"
dependencies = [
  "fastapi==0.115.12",
  "uvicorn[standard]==0.34.2",
]

[tool.uv]
package = false
